query {
    searchAuthorById(authorId: "${authorId}") {
        ... on Author {
            id
            firstName
            lastName
        }
        ... on BookStoreError {
            status
            message
        }
    }
}
