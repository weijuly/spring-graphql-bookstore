mutation {
    addCustomer(customer: {
        firstName: "GopiKrishnan",
        lastName: "Ganesan"
    }) {
        ... on Customer {
            id
            firstName
            lastName
        }
        ... on BookStoreError {
            message
            status
        }
    }
}
